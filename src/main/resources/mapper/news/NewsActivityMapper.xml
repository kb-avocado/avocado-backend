<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.avocado.domain.news.mapper.NewsActivityMapper">

    <resultMap id="newsActivityResultMap" type="com.avocado.domain.news.domain.NewsActivity">
        <id property="id" column="id"/>
        <result property="childId" column="child_id"/>
        <result property="articleId" column="article_id"/>
        <result property="childAnswer" column="child_answer"/>
        <result property="viewedAt" column="viewed_at"/>
        <result property="completedAt" column="completed_at"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findByChildIdAndArticleId" resultMap="newsActivityResultMap">
        SELECT id, child_id, article_id, child_answer, viewed_at, completed_at, created_at, updated_at
        FROM news_activities
        WHERE child_id = #{childId} AND article_id = #{articleId}
    </select>

    <select id="findByChildIdAndArticleIds" resultMap="newsActivityResultMap">
        SELECT id, child_id, article_id, child_answer, viewed_at, completed_at, created_at, updated_at
        FROM news_activities
        WHERE child_id = #{childId}
        AND article_id IN
        <foreach item="articleId" collection="articleIds" open="(" separator="," close=")">
            #{articleId}
        </foreach>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO news_activities
        (child_id, article_id, child_answer, viewed_at, completed_at, created_at, updated_at)
        VALUES
            (#{childId}, #{articleId}, #{childAnswer}, #{viewedAt}, #{completedAt}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update">
        UPDATE news_activities
        SET child_answer = #{childAnswer},
            viewed_at = #{viewedAt},
            completed_at = #{completedAt},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

</mapper>