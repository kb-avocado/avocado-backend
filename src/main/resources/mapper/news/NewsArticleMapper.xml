<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.avocado.domain.news.mapper.NewsArticleMapper">

    <resultMap id="newsArticleResultMap" type="com.avocado.domain.news.domain.NewsArticle">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="subtitle" column="subtitle"/>
        <result property="link" column="link"/>
        <result property="todayChallenge" column="today_challenge"/>
        <result property="publishedAt" column="published_at"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findList" resultMap="newsArticleResultMap">
        SELECT id, title, subtitle, link, today_challenge, published_at, created_at
        FROM news_articles
        ORDER BY published_at DESC, id DESC
            LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM news_articles
    </select>

    <select id="findById" resultMap="newsArticleResultMap">
        SELECT id, title, subtitle, link, today_challenge, published_at, created_at
        FROM news_articles
        WHERE id = #{id}
    </select>

    <select id="existsByLink" resultType="boolean">
        SELECT EXISTS(
            SELECT 1 FROM news_articles WHERE link = #{link}
        )
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="article_id">
        INSERT INTO news_articles (title, subtitle, link, today_challenge, published_at, created_at)
        VALUES (#{title}, #{subtitle}, #{link}, #{todayChallenge}, #{publishedAt}, #{createdAt})
    </insert>

</mapper>