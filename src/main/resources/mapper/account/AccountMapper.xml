<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.avocado.domain.account.mapper.AccountMapper">
    <!-- 은행 중복 계좌 여부 체크  -->
    <select id="existsByBankCodeAndAccountNumber" resultType="boolean">
        SELECT EXISTS (
        SELECT 1
        FROM accounts
        WHERE bank_code = #{bankCode}
        AND account_number = #{accountNumber}
        )
    </select>

    <insert id="insertAccount" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO accounts (
            user_id,
            bank_code,
            account_number
        )
        VALUES (
            #{userId},
            #{bankCode},
            #{accountNumber}
        )
    </insert>
</mapper>